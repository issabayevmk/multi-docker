version: "3"
services:
  client:
    image: "issabayevmk/multi-client"
    mem_limit: 128m
    hostname: client
  server:
    image: "issabayevmk/multi-server"
    mem_limit: 128m
    hostname: api
    environment:
      - REDIS_HOST=$REDIS_HOST
      - REDIS_PORT=$REDIS_PORT
      - PGUSER=$PGUSER
      - PGHOST=$PGHOST
      - PGDATABASE=$PGDATABASE
      - PGPASSWORD=$PGPASSWORD
      - PGPORT=$PGPORT
  worker:
    image: "issabayevmk/multi-worker"
    mem_limit: 128m
    hostname: worker
    environment:
      - REDIS_HOST=$REDIS_HOST
      - REDIS_PORT=$REDIS_PORT
  nginx:
    image: "issabayevmk/multi-ngnix"
    mem_limit: 128m
    hostname: ngnix
    ports:
     - "80:80"
  
